# train_smoke.py

## Overview

**Path:** `./scripts/train_smoke.py`

**Purpose:**  
scripts/train_smoke.py

Tiny "one-epoch" trainer to validate dataset shapes, dtypes and training pipeline.
Trains a single-epoch linear classifier (SGD) on regime_sig using basic features.

Usage:
  PYTHONPATH=. python scripts/train_smoke.py --data out/datasets_v02/labels_dataset_v02_<ts>_train.parquet

If `--data` omitted, defaults to latest CSV in out/datasets_v02 matching *_train.csv

## Primary Responsibilities

- `main()`

## Entry Points & CLI Usage

**CLI Framework:** argparse

**Example command:**
```bash
python train_smoke.py [args]
```

## Key Functions Called

## Feature & Label References

**Features:** `cycle_sig`, `regime_sig`, `reversal_sig`

**Labels:** _None detected_

## File I/O Operations

- Line 2: `
scripts/train_smoke.py

Tiny "one-epoch" trainer to validate dataset shapes, dtypes and training pipeline.
Trains a single-epoch linear classifier (SGD) on regime_sig using basic features.

Usage:
  PYTHONPATH=. python scripts/train_smoke.py --data out/datasets_v02/labels_dataset_v02_<ts>_train.parquet

If `--data` omitted, defaults to latest CSV in out/datasets_v02 matching *_train.csv
`
- Line 32: `.parquet`
- Line 25: `*_train.parquet`
- Line 25: `*_train.csv`
- Line 62: `load_df`
- Line 33: `read_parquet`
- Line 35: `read_csv`

## Output Formats

`csv`, `parquet`

## Side Effects

_No significant side effects detected_

## Runtime Expectations

**Estimated runtime class:** heavy (>5m)

## Test Coverage

_No tests found that reference this script_

## Notes & Recommendations


---
_Generated by script analyzer_
